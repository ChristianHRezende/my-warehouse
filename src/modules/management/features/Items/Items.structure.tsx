import { FeatureLayout } from '../../components';

const Items = () => {
  return (
    <FeatureLayout>
      <div>ITEMS</div>
    </FeatureLayout>
  );
};

export default Items;

// TODO: isolate signUp service
// TODO: build UI items page
